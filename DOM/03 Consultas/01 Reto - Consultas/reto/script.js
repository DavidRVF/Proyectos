const header = document.querySelectorAll('header');
const a = document.querySelectorAll('a');
const footer = document.querySelectorAll('footer');
const main = document.querySelectorAll('#contain .tarjeta')
console.log(header)
console.log(a)
console.log(footer)
console.log(main)